<div class="container mt-5">
  <div class="row">
    <div class="col-md-9">
      <!-- Tiêu đề và nút tạo mới -->
      <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="text-primary"><i class="fas fa-calendar-alt me-2"></i><%= t('events.title') %></h2>
        <% if current_user.system_role_id != 3 %>
          <a class="btn btn-primary" href="<%= new_event_path %>">
            <i class="fas fa-plus me-1"></i> <%= t('events.create_new') %>
          </a>
        <% end %>
      </div>

      <!-- Form tìm kiếm -->
      <div class="mb-4">
        <%= search_form_for @q, url: event_index_path, method: :get, html: { class: "d-flex align-items-center" } do |f| %>
          <div class="input-group">
            <span class="input-group-text bg-light"><i class="fas fa-search"></i></span>
            <%= f.text_field :name_cont, class: "form-control", placeholder: t('events.search_placeholder') %>
            <button class="btn btn-secondary">
              <i class="fas fa-search"></i> <%= t('events.search_button') %>
            </button>
          </div>
        <% end %>
      </div>

      <!-- Bảng danh sách sự kiện -->
      <div class="table-responsive shadow-sm">
        <table class="table table-striped table-hover">
          <thead class="table-dark">
          <tr>
            <th><%= t('events.table.number') %></th>
            <th><i class="fas fa-tag"></i> <%= t('events.table.name') %></th>
            <th><i class="fas fa-map-marker-alt"></i> <%= t('events.table.location') %></th>
            <th class="text-center"><i class="fas fa-cogs"></i> <%= t('events.table.actions') %></th>
          </tr>
          </thead>
          <tbody>
          <% @events.each_with_index do |event, index| %>
            <tr>
              <td><%= index + 1 %></td>
              <td><%= event.name %></td>
              <td><%= event.location %></td>
              <td class="text-center">
                <a href="<%= event_path(event) %>" class="btn btn-secondary btn-sm">
                  <i class="fas fa-eye"></i> <%= t('events.table.details') %>
                </a>
                <% if current_user.system_role_id == 1 %>
                  <%= link_to event_path(event), method: :delete, data: { confirm: t('events.table.confirm_delete') }, class: 'btn btn-danger btn-sm' do %>
                    <i class="fas fa-trash-alt"></i> <%= t('events.table.delete') %>
                  <% end %>
                <% end %>
              </td>
            </tr>
          <% end %>
          </tbody>
        </table>
      </div>

      <!-- Phân trang -->
      <div class="mt-4">
        <%= paginate @events %>
      </div>
    </div>
  </div>
</div>
