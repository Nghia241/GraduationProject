<nav class="navbar navbar-dark bg-dark">
  <div class="container-fluid">
    <!-- Logo hoặc tiêu đề -->
    <a class="navbar-brand d-block d-md-none vw-100 text-center" href="#">Hệ thống quản lý nội bộ</a>
    <a class="navbar-brand d-none d-md-block " href="#">Hệ thống quản lý nội bộ</a>
    <!-- Phần bên phải của navbar -->
    <div class="d-flex d-md-none w-100 justify-content-center align-items-center">
      <span class="navbar-text text-white me-3  ">
        Xin chào <%= current_user.name if defined?(current_user) %>
      </span>
      <!-- Nút toggle cho menu trên mobile -->
      <button class="navbar-toggler d-md-none" type="button" data-bs-toggle="collapse" data-bs-target="#mobileMenu" aria-controls="mobileMenu" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
    </div>
    <div class="d-flex d-none d-md-block">
      <span class="navbar-text text-white me-3 ">
        Xin chào <%= current_user.name if defined?(current_user) %>
      </span>
    </div>
  </div>

  <!-- Menu sidebar trên mobile (dùng collapse) -->
  <div class="collapse d-md-none bg-dark w-100" id="mobileMenu">
    <ul class="nav nav-pills flex-column p-3">
      <% if current_user.system_role_id == 1 %>
        <li class="nav-item">
          <%= link_to "Tổng quan", home_index_path, class: "nav-link text-white #{active_class(home_index_path)}" %>
        </li>
        <li class="nav-item">
          <%= link_to "Quản lý sự kiện", event_index_path, class: "nav-link text-white #{active_class(event_index_path)}" %>
        </li>
      <% end %>
      <% if current_user.system_role_id == 3 || current_user.system_role_id == 2 %>
        <li class="nav-item">
          <%= link_to "Quản lý sự kiện cá nhân", event_details_event_index_path, class: "nav-link text-white #{active_class(event_details_event_index_path)}" %>
        </li>
      <% end %>
      <li>
        <%= link_to "Tài khoản", profile_path, class: "nav-link text-white #{active_class(profile_path)}" %>
      </li>
      <% if current_user.system_role_id == 1 %>
        <li>
          <%= link_to "Quản lý thẩm quyền", role_manager_users_path, class: "nav-link text-white #{active_class(role_manager_users_path)}" %>
        </li>
      <% end %>
      <li>
        <%= link_to 'Đăng xuất', destroy_user_session_path, method: :delete, class: 'nav-link text-danger' %>
      </li>
    </ul>
  </div>
</nav>
